{
  "name": "react-with-axios",
  "version": "0.1.0",
  "description": "React HoC for consuming axios from context",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint -c tslint.json \"src/**/*.ts\"",
    "build": "tsc",
    "prepublish": "npm run clean && npm run build"
  },
  "author": "Giff Song <giff.song@gmail.com>",
  "license": "MIT",
  "repository": "pawsong/react-with-axios",
  "files": [
    "src",
    "lib"
  ],
  "dependencies": {
    "hoist-non-react-statics": "^1.2.0",
    "invariant": "^2.2.2"
  },
  "peerDependencies": {
    "axios": "^0.15.0"
  },
  "devDependencies": {
    "@types/invariant": "^2.2.28",
    "@types/react": "^15.0.12",
    "rimraf": "^2.6.0",
    "tslint": "^4.4.2",
    "typescript": "^2.1.6"
  }
}
